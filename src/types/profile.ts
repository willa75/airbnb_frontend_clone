export interface MyProfile {
  id: string;
  name: string;
  screenName: string;
  imageUrl?: string;
  reviews: number;
  reservations: number;
  listings: number;
  rating: number;
  favorites: number;
}